<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myblog.service.security.mapper.RoleMapper">

    <select id="getRolesByUserName" resultType="java.lang.String">

SELECT t3.role_name FROM t_admin t1
        LEFT JOIN t_role_admin t2 ON t1.id = t2.admin_id
        LEFT JOIN t_role t3 ON t2.role_id = t3.id
        WHERE t1.is_deleted = 0 AND t1.user_name=#{userName};
    </select>
</mapper>
